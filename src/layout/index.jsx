import React, { Fragment } from "react";
import { Navbar } from "../components/navbar";

export const Layout = () => {
    return(
        <Fragment>
            <Navbar />
        </Fragment>
    )
}